{
  "name": "orchard-phenology-atlas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4317",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4317",
    "typecheck": "vue-tsc --noEmit",
    "verify": "node scripts/verify.mjs",
    "check:hygiene": "node scripts/check_hygiene.mjs",
    "check:api": "node scripts/api_check.mjs",
    "check:workflow": "node scripts/workflow_check.mjs"
  },
  "dependencies": {
    "@lucide/vue": "^1.45.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "playwright": "^1.49.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vue-tsc": "^2.2.0"
  }
}
